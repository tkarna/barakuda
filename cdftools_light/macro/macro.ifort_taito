# Makefile for CDFTOOLS
# --------------------------------------------------------------

NCDF_DIR=$NETCDF_DIR

NCDF= -I$(NCDF_DIR)/include -L$(NCDF_DIR)/lib -lnetcdf

# -lnetcdff

F90=ifort

FFLAGS= -O3 -module ./mod $(NCDF)

